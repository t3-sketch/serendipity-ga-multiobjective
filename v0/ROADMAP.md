# v0のMicroロードマップ

最終更新：2026-09-11
関連Macro：[M1：システム側の比較](../ROADMAP.md#m1)。
ここでのStepはversion内の実験単位であり、研究全体のM1〜M5とは別である。

## 現在の到達点：MovieLens 1M

最新結果は1Mの本比較とPhase 1診断である。
100Kの接続実験だけで進捗を止めて読まないよう、先に現在の結果を示す。
成果物は別保存のままであり、ここへの掲載でversionの割り当てや移動は行わない。

| 実験 | 実行状態 | 結果と検証範囲 |
|---|---|---|
| 1M本比較 `ml-1m-rd-k10-resumed2` | 完了、non-smoke | K=10、候補100件。距離上昇とheld-out品質低下。保存済み完了記録と集計を照合 |
| 1M Phase 1 `ml-1m-rd-k10-phase1` | 完了、non-smoke | 同じcheckpointで探索を追跡。加重和との推薦完全一致は2,880/2,922組（98.56%）。保存済み集計と推薦CSVを照合 |

目的は平均`r`と平均`d`で、`r×d`は診断値である。
positiveは推薦時点までの履歴平均超え（validationはtrain、testはtrain＋validation）とする。
Test Candidate Recall@100は0.192242、NDCG@10はSASRec 0.095103、加重和0.050995、NSGA-II 0.051279だった。
NSGA-IIのNDCG上の優位性は確認できず、代理指標の上昇をexperienced serendipityの改善とは扱わない。

確認日は2026-09-11。保存済み結果の照合に加え、既存`check.py`でPhase 1の内部整合性と本比較からの候補・推薦・既存指標の回帰一致を再確認した。
学習と実験は再実行していない。
詳細は[1Mの保存記録](../ROADMAP.md#external-1m)と[研究記事](README.md)を参照する。
1Mで確認した推薦長はK=10であり、K=15、20、25、30の完了は未確認である。
M1の完了判定とcanonical評価条件の採否も未完である。

## 過去の100K Stepと結果

| ID | Research Question | 実行状態 | 簡潔な結果 | 検証状態 | 報告 |
|---|---|---|---|---|---|
| v0-S1 | 同じ候補からproxyを考慮した推薦を選ぶと、SASRecと比べて何が変わるか | 完了（旧100K、K=10） | proxy上昇、NDCGとRecall低下。NSGA-II代表解は加重和と一致 | 保存集計を照合。実験当時の検証記録あり。再実行は未実施 | [S01](reports/S01-main.md) |
| v0-S2 | K=10の結果は推薦リストの短さに依存するか | 完了（旧100K、K=10〜30） | 全Kで同じ傾向。長さだけでは乖離を解消せず | 保存集計と一致件数を照合。再実行は未実施 | [S02](reports/S02-list-length.md) |
| v0-S3 | 履歴平均positiveと目的(r,d)への変更後も実行と評価が接続するか | 100Kの接続確認完了。100K本比較は未完 | 100K smoke完了。1M本比較は上記を参照 | 完了マーカーと分割記録を照合。過去のcheck実行記録あり | [S03](reports/S03-definition-smoke.md) |

計画記録は[plansの入口](plans/README.md)を参照する。
上の3件は既存実験の事後整理であり、当時の独立した事前計画書と承認の対応は未確認である。
新しい100K本実験の計画や実行は、今回の文書整理には含めない。

## 過去の100K Stepの完了条件

### v0-S1

- [x] K=10の本比較と設定を保存した。
- [x] 同じ候補上で加重和との比較を残した。
- [x] 結果と解釈、主張できない範囲を報告した。
- [ ] 当時のコードと環境を復元した再実行による再現確認（未実施。実行完了とは別）。

### v0-S2

- [x] 共通モデルでK=10、15、20、25、30の結果を保存した。
- [x] 候補一致とK=10の既存指標一致の検証記録を保存した。
- [x] 同じKで手法を比べ、K間比較の限界を報告した。
- [ ] 当時のコードを復元した全条件の再実行による再現確認（未実施）。

### v0-S3

- [x] positiveを推薦時点までの本人の平均rating超えとした。
- [x] 目的を平均(r,d)とし、r×dを診断値へ分離した。
- [x] 100Kのsmokeと対象人数の変更を記録した。
- [ ] 現条件をcanonical baselineとして採用するか判断する。
- [ ] 本比較を必要と判断した場合、計画を承認して別出力先で実行する。
- [ ] 本比較の結果を検証して報告する（現時点では未完）。

## 次の研究判断

主作業は[Macro M1](../ROADMAP.md#m1)の既存1M実験と評価設計の照合である。
1Mをv0やv1へ自動的に割り当てず、その結果を100Kのv0-S3完了の証拠に使わない。
旧実験の失敗した仮説も保存し、後続の条件変更とは別のreportに残す。
